services:
  nginx:
    image: nginx:latest
    container_name: nginx
    volumes:
     - ./:/var/www
     - ./nginx.conf:/etc/nginx/nginx.conf
    ports:
      - 80:80
    networks:
      - my-network
    depends_on:
      - flask
  flask:
    build:
      context: .
      dockerfile: Dockerfile.flask
    container_name: flask
    command: gunicorn --bind 0.0.0.0:8000 --workers 4 "server/app:app"
    volumes:
      - ./:/var/www
    networks:
      my-network:
        aliases:
          - flask-app

networks:
  my-network: